// Namespaced
const namespaced = true

// State
const state = {
    all: []
}

// Getters
const getters = {
    all: state => state.all
}

// Actions
const actions = {
    search({ commit, dispatch }, { where, bedroomCount, redirect }) {

    }
}

// Mutations
const mutations = {

}

export default {
    namespaced,
    state,
    getters,
    actions,
    mutations
}
